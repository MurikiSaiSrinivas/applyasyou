# Resume corpus catalog

The agent fills this during onboarding (CLAUDE.md Section 1, Step 3) by reading
everything in `resumes/`. One row per variant: which file, its angle, and when to
use it. Keep it in sync with `config.json` -> `resume_clusters`.

| Variant | File | Angle | Use when |
|---|---|---|---|
| _(populated from your resumes during onboarding)_ | | | |
